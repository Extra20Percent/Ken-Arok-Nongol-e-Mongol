using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class WaveCounter : MonoBehaviour {

	public Text wavesCount;

	void Update ()
	{
		wavesCount.text = "Waves: " + PlayerStats.wavesCount.ToString();
	}

}
